
- if @job.complete
	= render :partial => 'layouts/notice', :locals => { :message => 'This job has already been completed.' }

= render @job

%h3.indent
	bids
	.pull-right
		= link_to "make a bid &raquo;".html_safe, new_job_bid_path(@job) + '?job_id=' + @job.id.to_s()

= render @job.bids
